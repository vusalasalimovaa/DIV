import React from 'react'
import Home from './Home'

const Products = ({products,handleClick}) => {
  return (
    <div>
      {/* <Home products={products} handleClick={handleClick}/> */}
      Products
    </div>
  )
}

export default Products